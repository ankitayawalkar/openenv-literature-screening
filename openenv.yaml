name: clinical-literature-screening-env

tasks:
  - name: easy
    description: Binary classification (include/exclude)

  - name: medium
    description: Classification with exclusion reason

  - name: hard
    description: High precision classification with penalties

entrypoint:
  reset: /reset
  state: /state
  step: /step

grader:
  endpoint: /grader

metadata:
  author: Ankita
  domain: healthcare
