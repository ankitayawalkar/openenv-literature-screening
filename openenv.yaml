name: literature_screening_env
description: Clinical literature screening environment
version: "1.0"

environment:
  endpoints:
    reset: /reset
    state: /state
    step: /step

grader:
  endpoint: /grader

tasks:
  - id: easy
    description: Binary classification

  - id: medium
    description: Classification with reason

  - id: hard
    description: Strict grading
